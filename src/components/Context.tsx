import React, { useContext } from 'react'
//import { hello } from '../App';

function Context() {
    //const useHello = useContext(hello);
    return (
        <div>
            {/* {useHello.name} */}
        </div>
    )
}

export default Context
